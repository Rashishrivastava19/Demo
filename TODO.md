# TODO: Remove Horizontal Scroll and Center Pages

## Steps to Complete
- [ ] Edit `frontend/HireHelper/src/Styles/App.css` to add `overflow-x: hidden` to `.app-container`, `.dashboard-layout`, and `.main-content`.
- [ ] Edit `.main-content` to add centering styles: `display: flex; justify-content: center; flex-direction: column; align-items: center;`.
- [ ] Edit `.left-panel` to add centering: `align-items: center; text-align: center;`.
- [ ] Optionally add `max-width: 1400px; margin: 0 auto;` to `.app-container` for centering on wide screens.
- [ ] Test changes: View login and dashboard pages to confirm no horizontal scroll and content is centered.
